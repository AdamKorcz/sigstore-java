---
name: SLSA Provenance
on:
  - workflow_dispatch

permissions: read-all

env:
  GH_TOKEN: ${{ github.token }}
  ISSUE_REPOSITORY: ${{ github.repository }}
jobs:
  usetrw:
    permissions:
      contents: write
      id-token: write
      actions: read   # For the entrypoint.
      packages: write
    uses: AdamKorcz/java-slsa-generator/.github/workflows/gradle-trw.yml@main
    with:
      name1: value1
      name2: value2
      rekor-log-public: true
      artifact-list: "./sigstore-java/build/local-maven-repo/dev/sigstore/sigstore-java/0.4.0/sigstore-java-0.4.0.module,./sigstore-java/build/libs/sigstore-java-0.4.0.jar"
